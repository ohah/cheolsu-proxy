{
  "name": "cheolsu-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "A simple Man In The Middle proxy built with Rust and Tauri",
  "keywords": [
    "documentation",
    "mitm",
    "proxy",
    "rust",
    "tauri"
  ],
  "license": "MIT",
  "author": "Cheolsu Proxy Team",
  "workspaces": [
    "tauri-ui",
    "document"
  ],
  "type": "module",
  "scripts": {
    "dev": "bun run --filter cheolsu-proxy-document dev",
    "build": "bun run --filter cheolsu-proxy-document build",
    "preview": "bun run --filter cheolsu-proxy-document preview",
    "tauri:dev": "bun --cwd tauri-ui tauri dev",
    "tauri:build": "bun --cwd tauri-ui tauri build",
    "tauri:headless": "cd tauri-ui/src-tauri && cargo run --no-default-features -- --headless --port 8100",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "format": "oxfmt",
    "format:check": "oxfmt --check"
  },
  "devDependencies": {
    "oxfmt": "latest",
    "oxlint": "latest"
  },
  "packageManager": "bun@latest"
}
